{% extends "base.jinja2" %}

{% block container %}

<div class="columns">
  <div class="column is-1">
  </div>
  <div class="column is-10">
    <div class="content">
      <h1 class="title">{{ page.title }}</h1>

      {% include 'messages.jinja2' %}

      <p>Soll die Order gelöscht werden?</p>
      <p>
        <a href="{{ url_for('order.delete', order_id=order.id, confirm='yes' ) }}"><button class="button is-danger">
          <span class="icon">
            <i class="fas fa-trash"></i>
          </span>
          <span>Delete</span>
        </button></a>
        <a href="{{ url_for('order.dashboard') }}"><button class="button is-primary">No</button></a>
      </p>
    </div>
  </div>
  <div class="column is-1">
  </div>
</div>

{% endblock %}
